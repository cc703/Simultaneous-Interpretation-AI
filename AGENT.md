# AGENT.md — AI 同声传译助手开发契约

本项目选择比赛题目二：**AI 同声传译助手**。

目标是在 2026-06-05 00:00 至 2026-06-07 23:59 的 72 小时开发周期内，完成一个能将外语音频流实时翻译成中文，并支持字幕/语音输出与翻译修正能力的 AI 应用。

详细产品设计、PR 拆分、测试方式和提交节奏见 `design.md`。本文件是执行时的简明约束，任何实现都必须服从 `design.md` 中的题目二目标和评审规则。

---

## 1. 项目目标

必须紧贴题目二，不开发英语口语陪练、小说转剧本、泛聊天机器人等偏题功能。

核心产品闭环：

1. 输入英文音频、示例音频或实时语音。
2. 按时间顺序显示英文原文和中文字幕。
3. 中文字幕以实时或准实时方式出现。
4. 用户可以修正任意一句译文。
5. 用户可以维护术语表，后续翻译优先遵守术语表。
6. 支持导出 SRT 或双语文本。
7. README 和 Demo 视频清楚展示：音频输入 -> 实时字幕 -> 人工修正 -> 术语生效 -> 导出。

---

## 2. 执行原则

- 先读 `design.md`，再动手实现。
- 按 `design.md` 的 PR 顺序推进，不跨 PR 合并多个大功能。
- 主分支 `main` 必须始终可运行。
- 每个功能分支只实现一个单一功能。
- 不直接向 `main` 提交；通过 PR 合并。
- 每完成一个可验证小步骤就提交一次 Commit，不能等最后一天集中提交。
- 如果功能因时间限制只能完成阶段性版本，必须在 README 和 PR 描述中写清边界。
- 不夸大能力，尤其不要把 Web Speech API 描述成已支持任意文件/系统音频流直接识别。

---

## 3. 强制 Git 工作流

每个 PR 单元使用以下流程：

```bash
git switch main
npm run build

git switch -c feat/<feature-name>

# 完成一个最小可验证步骤后立刻提交
git add -A
git commit -m "feat(scope): 完成某个可验证小步骤"

# PR 单元完成后验证
npm run build

git push -u origin feat/<feature-name>
```

Commit 要清晰说明进展，禁止使用：

```bash
git commit -m "update"
git commit -m "fix"
git commit -m "改了一下"
```

推荐 Commit 类型：

| 类型 | 用途 |
|---|---|
| `feat` | 新增同传功能、UI、引擎、修正能力 |
| `fix` | 修复识别、翻译、字幕、构建问题 |
| `docs` | 更新 README、Demo 脚本、设计说明 |
| `test` | 增加测试或验证脚本 |
| `chore` | 配置、依赖、脚手架维护 |

---

## 4. PR 要求

每个 PR 必须包含：

```md
标题：[feat] 一句话说明本 PR 新增/修改了什么

## 功能描述
说明该功能的作用与使用方式。

## 实现思路
说明核心技术方案、状态流、组件或引擎设计。

## 测试方式
写清具体执行步骤，不能只写“自测通过”。

## Commit 记录
- `feat(scope): ...`
- `docs(scope): ...`
```

PR 完成条件：

1. 功能达到对应 PR 目标。
2. 至少有一个清晰 Commit。
3. `npm run build` 通过；若无法运行，必须说明原因。
4. README 或相关文档已按需要同步。
5. 合并后 `main` 仍可运行。

---

## 5. 产品能力边界

必须诚实区分真实能力、Demo 能力和扩展接口：

| 能力 | 可以说明 | 不允许夸大 |
|---|---|---|
| 麦克风模式 | 使用 Web Speech API 实时识别麦克风英文语音并翻译 | 支持所有浏览器、所有语言、无延迟 |
| 文件模式 MVP | 支持示例音频按时间轴驱动转写流，展示字幕、修正和导出闭环 | 已完整识别任意文件音频，除非 ASR Adapter 已实现并验证 |
| 直播模式 MVP | 支持捕获标签页/屏幕音频并显示波形，预留 ASR 分片接口 | 已把系统音频直接送入 Web Speech API 识别 |
| Demo 模式 | 用于评审稳定演示完整产品闭环 | 用 Demo 假装真实能力已全部完成 |

---

## 6. 验证要求

每次实现后优先运行：

```bash
npm run build
```

涉及 UI 的功能还要在浏览器中验证：

- 页面能打开。
- 字幕区域不空白。
- 状态栏、按钮、输入源切换正常。
- 修正、术语表、导出等核心交互可执行。

最终 Demo 必须展示：

1. 外语音频或 Demo 音频开始播放。
2. 英文原文和中文字幕按时间出现。
3. 手动修正一条译文。
4. 添加一个术语并让后续翻译/重译生效。
5. 导出最终双语字幕或 SRT。

---

## 7. 交付物

最终必须提交：

- 公开 GitHub/Gitee 仓库。
- README.md。
- Demo 视频链接。
- 持续 Commit 记录。
- 按单一功能拆分的 PR 记录。

README 必须明确写出：

- 本作品选择题目二：AI 同声传译助手。
- 快速开始步骤。
- 功能截图或说明。
- 支持的 Provider。
- Demo 视频链接。
- 真实模式和 Demo 模式的能力边界。
- 已知限制与后续扩展方向。
